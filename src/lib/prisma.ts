/**
 * Prisma stub — the app now uses the file-based user-store.ts.
 * This file is kept to avoid breaking any imports that may reference it.
 * Nothing here is actually used.
 */

export const prisma = null;
